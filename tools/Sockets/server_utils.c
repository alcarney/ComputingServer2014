#include "server_utils.h"


// This sets up the server so that we're ready to send/receive data 
// returns the socket file descriptor that is used to communicate with
// the loopback interface - Hopefully that's all we need for this to work
struct server_info* setup_server()
{
    
}
